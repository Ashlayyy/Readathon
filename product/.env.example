# Product site (product.com) — marketing + host panel
VITE_PRODUCT_NAME=Product
VITE_PRODUCT_APEX=product.com
# Where player sites live in local/dev (Book Baddies / path tenants)
VITE_PLAYER_ORIGIN=http://localhost:5173
# Leave empty in dev (Vite proxies /api → server)
# VITE_API_URL=
